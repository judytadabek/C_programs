#include <stdio.h>
int main ()
{
	float m1, m2, c1, c2;
	
	printf("Enter the values of m1, m2, c1, c2:\n");
	scanf("%f %f %f %f", &m1, &m2, &c1, &c2);
	
	
	printf("X-coordinate of the point where the two line cross is: %f \n", (c2-c1)/(m1-m2));
	
	float x;
	
	x = (c2-c1)/(m1-m2); 
	
	printf("Y-coordinate of the point: %f \n", m1*x + c1);
	
	return 0;
	
}
	
